# Versioning contract

Framework, Skill Pack, individual skill, and installed Workspace schema versions are tracked independently. Unknown newer Workspace versions are read-only and are never automatically downgraded.

The public Skill Pack version is `0.1.0-beta.1`. The compatibility manifest records the supported framework versions and the target fallback framework used by Phase 1 tooling.
