# Distribution source

The canonical creator is stored in ordered chunks under `source/creator/` to keep connector writes reliable. Run `bash scripts/build_releases.sh` to materialize the complete release files.
